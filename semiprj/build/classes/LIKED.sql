DROP TABLE LIKED
CASCADE CONSTRAINTS ;

DROP SEQUENCE SEQ_LIKED ;

CREATE TABLE LIKED(
  SEQ NUMBER(10) PRIMARY KEY ,
  ID VARCHAR2(50) NOT NULL,
  SEQ_RESTAURANT NUMBER(10) NOT NULL
);

CREATE SEQUENCE SEQ_LIKED
START WITH 1
INCREMENT BY 1;

ALTER TABLE LIKED
ADD CONSTRAINT FK_LIKED_ID FOREIGN KEY (ID)
REFERENCES MEMBER(ID);

ALTER TABLE LIKED
ADD CONSTRAINT FK_LIKED_SEQ_RESTAURANT FOREIGN KEY (SEQ_RESTAURANT)
REFERENCES RESTAURANT_BBS(SEQ);
